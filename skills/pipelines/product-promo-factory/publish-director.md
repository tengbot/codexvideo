# Publish Director

Export the final video with captions, script, product truth, pain library, value map,
hook candidates, creative brief, proof plan, asset sources, render report, and QA.

Do not report platform performance as part of production. After real distribution,
record impressions, three-second views, outbound clicks, conversions, and spend in an
`experiment_result` and run `promo_strategy diagnose` with campaign-specific thresholds.
