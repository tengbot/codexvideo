# AI Dialogue Podcast Executive Producer

Read `skills/core/content-formats.md`, the pipeline manifest, the next stage
director, reviewer, and checkpoint protocol before acting.

Run preflight. A real two-host deliverable requires two distinct voice paths, a
usable avatar or performance-generation path for both hosts, subtitle support,
and a composition runtime. A static portrait with an audio waveform is a degraded
prototype and must not be presented as a finished avatar podcast.

Execute `research -> proposal -> script -> scene_plan -> assets -> edit -> compose
-> publish`. Preserve cast identities, voices, studio continuity, approved hook,
language, aspect ratio, and render runtime. Regenerate only failed turns or shots
when possible.

